type: object
properties:
  errorCode:
    type: integer
    format: int32
    description: Error code.
    example: 400
  errorMessage:
    type: string
    description: Error message
    example: Bad Request
  required:
    - errorCode
    - errorMessage
examples:
  - inputError:
      errorCode: 400
      errorMessage: Bad Request
